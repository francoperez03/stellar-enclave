{
  "name": "@enclave/demo",
  "version": "0.0.0",
  "private": true,
  "description": "Enclave demo Next.js app hosting the gated endpoint (Phase 4 target).",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@enclave/core": "*",
    "@enclave/gate": "*"
  },
  "devDependencies": {
    "typescript": "5.6.3"
  }
}
