{
  "name": "@enclave/gate",
  "version": "0.0.0",
  "private": true,
  "description": "Enclave Gate middleware source — withEnclaveGate({ orgId }) for Next.js (Phase 4 target).",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --project unit",
    "test:unit": "vitest run --project unit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@enclave/core": "*",
    "pino": "^9.14.0"
  },
  "devDependencies": {
    "typescript": "5.6.3",
    "vitest": "^3.2.4",
    "@types/express": "^5.0.0",
    "@types/node": "22.9.0"
  }
}
