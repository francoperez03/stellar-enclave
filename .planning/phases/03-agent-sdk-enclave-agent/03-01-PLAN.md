---
phase: 03-agent-sdk-enclave-agent
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/agent/package.json
  - packages/agent/jest.config.js
  - packages/agent/src/types.ts
  - packages/agent/src/logger.ts
  - packages/agent/src/config.ts
  - packages/agent/src/prover.ts
  - packages/agent/src/note-selector.ts
  - packages/agent/src/fetch-interceptor.ts
  - packages/agent/src/index.ts
  - packages/agent/src/__tests__/logger.test.ts
  - packages/agent/src/__tests__/prover.test.ts
  - packages/agent/src/__tests__/witness.test.ts
  - packages/agent/src/__tests__/fetch-interceptor.test.ts
  - .gitignore
autonomous: true
requirements: [SDK-01, SDK-02, SDK-03, SDK-04, SDK-05, SDK-06, SDK-07]

must_haves:
  truths:
    - "Jest test suite in packages/agent/ runs and reports results (tests may be stubs/skipping)"
    - "All agent source module files exist under packages/agent/src/"
    - "`.enclave.json` and `*-notes.json` files are blocked by .gitignore"
    - "EnclaveNote and AgentBundle types are exported from packages/agent/src/types.ts"
  artifacts:
    - path: "packages/agent/jest.config.js"
      provides: "Jest + ts-jest config for ESM TypeScript package"
    - path: "packages/agent/src/types.ts"
      provides: "EnclaveNote, AgentBundle, FixtureEntry, FixtureIndex types"
    - path: "packages/agent/src/__tests__/logger.test.ts"
      provides: "Stub test for SDK-06 redaction"
    - path: "packages/agent/src/__tests__/prover.test.ts"
      provides: "Stub tests for SDK-02, SDK-03, SDK-04"
    - path: "packages/agent/src/__tests__/witness.test.ts"
      provides: "Stub test for SDK-07 Model X"
    - path: "packages/agent/src/__tests__/fetch-interceptor.test.ts"
      provides: "Stub tests for SDK-01"
  key_links:
    - from: "packages/agent/jest.config.js"
      to: "packages/agent/src/__tests__/*.test.ts"
      via: "ts-jest transform"
      pattern: "transform.*ts-jest"
    - from: "packages/agent/src/index.ts"
      to: "packages/agent/src/types.ts"
      via: "re-export"
      pattern: "export.*EnclaveNote|AgentBundle"
---

<objective>
Bootstrap the @enclave/agent package with Jest + ts-jest test infrastructure, all source module stubs, and the complete types/interfaces that downstream tasks will implement against.

Purpose: Wave 0 work — establish contracts and test scaffolding so that Wave 1 tasks can implement against defined types with runnable tests.
Output: Jest config, all source stubs (non-throwing, just correct types), 4 test files with describe/it stubs that PASS (skip), .gitignore additions, devDeps installed.
</objective>

<execution_context>
@/Users/francoperez/.claude/get-shit-done/workflows/execute-plan.md
@/Users/francoperez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-agent-sdk-enclave-agent/03-CONTEXT.md
@.planning/phases/03-agent-sdk-enclave-agent/03-RESEARCH.md
</context>

<interfaces>
<!-- Key types already in @enclave/core that the agent imports -->

From packages/core/src/types.ts:
```typescript
export type OrgId = string;
export type OrgSpendingPubKey = Uint8Array;
export type AgentAuthKey = Uint8Array;

export interface ShieldedProof {
  proof: Uint8Array;
  root: bigint;
  inputNullifiers: bigint[];
  outputCommitment0: bigint;
  outputCommitment1: bigint;
  publicAmount: bigint;
  extDataHash: Uint8Array;
  aspMembershipRoot: bigint;
  aspNonMembershipRoot: bigint;
}

export interface PaymentRequest {
  payTo: string;
  maxAmountRequired: bigint;
  resource: string;
  nonce: string;
}
```

Phase 0 stub in packages/agent/src/index.ts:
```typescript
export const PHASE_0_STUB = true;
export type AgentConfig = {
  orgSpendingPubKey: OrgSpendingPubKey;
  authKey: AgentAuthKey;
  facilitatorUrl: string;
};
export interface Agent {
  fetch(url: string, init?: unknown): Promise<Response>;
}
export function createAgent(_config: AgentConfig): Agent {
  throw new Error("@enclave/agent: Phase 3 target, not yet implemented");
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install devDeps, add Jest config, update .gitignore</name>
  <read_first>
    - packages/agent/package.json (current devDeps and scripts)
    - packages/agent/tsconfig.json (extends base — needed for ts-jest config)
    - .gitignore (check what's already blocked)
    - app/package.json (verify jest version in use: 29.7.0)
  </read_first>
  <action>
**1a. Update packages/agent/package.json** — add devDependencies and test script:

```json
{
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist",
    "test": "jest --no-coverage",
    "test:watch": "jest --watch --no-coverage"
  },
  "devDependencies": {
    "typescript": "5.6.3",
    "@types/jest": "^29.5.12",
    "@types/node": "^22.0.0",
    "jest": "29.7.0",
    "ts-jest": "^29.2.5"
  }
}
```

Keep `"@enclave/core": "*"` in `dependencies` unchanged.

**1b. Create packages/agent/jest.config.js** with this exact content:

```js
export default {
  preset: 'ts-jest/presets/default-esm',
  testEnvironment: 'node',
  extensionsToTreatAsEsm: ['.ts'],
  moduleNameMapper: {
    '^(\\.{1,2}/.*)\\.js$': '$1',
  },
  transform: {
    '^.+\\.tsx?$': ['ts-jest', {
      useESM: true,
      tsconfig: {
        module: 'ESNext',
        moduleResolution: 'bundler',
      },
    }],
  },
  testMatch: ['**/__tests__/**/*.test.ts'],
  roots: ['<rootDir>/src'],
};
```

**1c. Update .gitignore** — add these two lines under the "Node workspaces" section (after the existing `*.key` line):

```
*.enclave.json
*-notes.json
```

**1d. Install new devDeps** from the repo root:

```bash
cd /Users/francoperez/repos/stellar-projects/stellar-enclave && npm install -w @enclave/agent
```
  </action>
  <verify>
    <automated>cd /Users/francoperez/repos/stellar-projects/stellar-enclave && cat packages/agent/package.json | grep -E '"jest"|"ts-jest"|"@types/jest"' && cat packages/agent/jest.config.js | grep "ts-jest" && git check-ignore -v test.enclave.json packages/agent/test.enclave.json 2>/dev/null || grep "\.enclave\.json" .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - packages/agent/package.json contains `"jest": "29.7.0"` under devDependencies
    - packages/agent/package.json contains `"ts-jest"` under devDependencies
    - packages/agent/jest.config.js contains the string `ts-jest/presets/default-esm`
    - .gitignore contains `*.enclave.json`
    - .gitignore contains `*-notes.json`
  </acceptance_criteria>
  <done>Jest + ts-jest installed, jest.config.js present, gitignore updated with bundle/notes patterns</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create all source module stubs + agent types</name>
  <read_first>
    - packages/agent/src/index.ts (Phase 0 stub — understand what to preserve and replace)
    - packages/core/src/types.ts (ShieldedProof, PaymentRequest, OrgSpendingPubKey, AgentAuthKey)
    - .planning/phases/03-agent-sdk-enclave-agent/03-CONTEXT.md (locked AgentBundle format, EnclaveNote concept, FixtureIndex format)
    - .planning/phases/03-agent-sdk-enclave-agent/03-RESEARCH.md (module structure recommendation, witness input shape)
  </read_first>
  <behavior>
    - logger.test.ts: describe("Logger redaction") { it.todo("redacts orgSpendingPrivKey") }
    - prover.test.ts: describe("Prover") { it.todo("returns 128-byte proof") it.todo("loads from local path") }
    - witness.test.ts: describe("Witness (Model X)") { it.todo("two agents from same org share input keypair") }
    - fetch-interceptor.test.ts: describe("agent.fetch()") { it.todo("returns response when no 402") it.todo("completes 402->prove->settle->retry cycle") it.todo("throws EnclavePaymentError on retry 402") }
  </behavior>
  <action>
Create or overwrite the following files:

**packages/agent/src/types.ts** — new file with agent-specific types:

```typescript
import type { ShieldedProof, PaymentRequest } from '@enclave/core';

/** A shielded note (UTXO) from the pool, held by an agent */
export interface EnclaveNote {
  /** Pool commitment hash as decimal string */
  commitment: string;
  /** Precomputed nullifier as decimal string */
  nullifier: string;
  /** Note amount in stroops */
  amount: bigint;
  /** Blinding factor as decimal string */
  blinding: string;
  /** Merkle path elements (array of TREE_DEPTH decimal strings) */
  pathElements: string[];
  /** Merkle path index as decimal string */
  pathIndex: string;
  /** ASP membership leaf (Poseidon2(orgSpendingPubKey, blinding=0)) as decimal string */
  aspLeaf: string;
  /** ASP membership Merkle path elements (array of decimal strings) */
  aspPathElements: string[];
  /** ASP membership path index as decimal string */
  aspPathIndex: string;
}

/** Agent bundle loaded from <agentName>.enclave.json */
export interface AgentBundle {
  /** Org spending private key as hex string (BN254 scalar, little-endian) */
  orgSpendingPrivKey: string;
  /** Agent auth key as hex string (used in Authorization: Bearer header) */
  agentAuthKey: string;
  /** Org identifier slug */
  orgId: string;
  /** Facilitator base URL */
  facilitatorUrl: string;
}

/** A pre-generated proof fixture entry (indexed by URL) */
export interface FixtureEntry {
  proof: ShieldedProof;
  extData: ExtData;
  note: { commitment: string; nullifier: string };
}

/** Fixture index: keyed by URL string */
export type FixtureIndex = Record<string, FixtureEntry>;

/** ExtData for pool.transact (mirrors upstream contracts/pool/src/pool.rs ExtData struct) */
export interface ExtData {
  /** Stellar address of the USDC recipient (as strkey) */
  recipient: string;
  /** External amount (negative for withdrawal, in stroops as BigInt) */
  extAmount: bigint;
  /** 112-byte encrypted output for real output (base64 or hex) */
  encryptedOutput1: Uint8Array;
  /** 112-byte dummy padding output (base64 or hex) */
  encryptedOutput2: Uint8Array;
}

/** Error thrown by agent.fetch() on payment failure */
export class EnclavePaymentError extends Error {
  reason: 'proof_failed' | 'facilitator_rejected' | 'no_funds' | 'retry_402';
  nullifier?: string;
  facilitatorResponse?: unknown;

  constructor(opts: {
    reason: EnclavePaymentError['reason'];
    nullifier?: string;
    facilitatorResponse?: unknown;
  }) {
    super(`EnclavePaymentError: ${opts.reason}`);
    this.name = 'EnclavePaymentError';
    this.reason = opts.reason;
    this.nullifier = opts.nullifier;
    this.facilitatorResponse = opts.facilitatorResponse;
  }
}

export type { ShieldedProof, PaymentRequest };
```

**packages/agent/src/logger.ts** — stub (real implementation in Plan 03-02):

```typescript
// Phase 3 Plan 02 target — real pino logger with redact paths
export const logger = {
  info: (_obj: unknown, _msg?: string) => {},
  warn: (_obj: unknown, _msg?: string) => {},
  error: (_obj: unknown, _msg?: string) => {},
  debug: (_obj: unknown, _msg?: string) => {},
};
export type Logger = typeof logger;
```

**packages/agent/src/config.ts** — stub:

```typescript
// Phase 3 Plan 02 target — bundle + notes loader with env var parsing
export type AgentConfig = {
  orgSpendingPrivKey: string;
  agentAuthKey: string;
  orgId: string;
  facilitatorUrl: string;
  provingArtifactsPath: string;
  notesPath: string;
  fixturePath?: string;
};
export async function loadConfig(): Promise<AgentConfig> {
  throw new Error('@enclave/agent config: Phase 3 Plan 02 target');
}
```

**packages/agent/src/prover.ts** — stub:

```typescript
// Phase 3 Plan 03 target — WASM prover wrapper
export type ProverHandle = { prover: unknown; witnessCalc: unknown };
export async function loadProverArtifacts(_path: string): Promise<ProverHandle> {
  throw new Error('@enclave/agent prover: Phase 3 Plan 03 target');
}
export async function prove(_handle: ProverHandle, _witnessInputsJson: string): Promise<{ proofBytes: Uint8Array; publicInputBytes: Uint8Array }> {
  throw new Error('@enclave/agent prove: Phase 3 Plan 03 target');
}
```

**packages/agent/src/note-selector.ts** — stub:

```typescript
// Phase 3 Plan 05 target — greedy note selector
import type { EnclaveNote } from './types.js';
export function selectNote(_notes: EnclaveNote[], _amount: bigint, _spentNullifiers: Set<string>): EnclaveNote | null {
  throw new Error('@enclave/agent note-selector: Phase 3 Plan 05 target');
}
```

**packages/agent/src/fetch-interceptor.ts** — stub:

```typescript
// Phase 3 Plan 05 target — 402 intercepting fetch
export async function createInterceptingFetch(_config: unknown): Promise<(url: string, init?: RequestInit) => Promise<Response>> {
  throw new Error('@enclave/agent fetch-interceptor: Phase 3 Plan 05 target');
}
```

**packages/agent/src/index.ts** — replace Phase 0 stub with real exports (implementations still throw in stubs):

```typescript
// @enclave/agent — Phase 3 implementation.
// Wave 0: types + stubs. Plans 02-05 replace the stub bodies.

export { EnclavePaymentError } from './types.js';
export type { EnclaveNote, AgentBundle, FixtureEntry, FixtureIndex, ExtData } from './types.js';
export type { ShieldedProof, PaymentRequest } from '@enclave/core';

export type AgentConfig = {
  /** Path to <agentName>.enclave.json bundle file */
  bundlePath?: string;
  /** Path to notes JSON file */
  notesPath?: string;
  /** Path to proving artifacts directory (wasm-prover-nodejs + wasm-witness-nodejs outputs) */
  provingArtifactsPath?: string;
  /** Optional path to fixture JSON (bypasses live proving when set) */
  fixturePath?: string;
};

export interface Agent {
  fetch(url: string, init?: RequestInit): Promise<Response>;
}

export async function createAgent(_config?: AgentConfig): Promise<Agent> {
  throw new Error('@enclave/agent: Phase 3 Plan 05 target — not yet implemented');
}

// Removed: PHASE_0_STUB — Phase 3 replaces the stub marker
```

**Create all 4 test stub files:**

**packages/agent/src/__tests__/logger.test.ts:**

```typescript
import { describe, it } from '@jest/globals';

describe('Logger redaction (SDK-06)', () => {
  it.todo('redacts orgSpendingPrivKey from log output');
  it.todo('redacts agentAuthKey from log output');
  it.todo('redacts proof.a, proof.b, proof.c from log output');
  it.todo('redacts inputNullifiers from log output');
  it.todo('passes through non-secret fields (orgId, url, phase)');
  it.todo('[Redacted] sentinel appears in output when secret is present');
});
```

**packages/agent/src/__tests__/prover.test.ts:**

```typescript
import { describe, it } from '@jest/globals';

describe('Prover wrapper (SDK-02, SDK-03, SDK-04)', () => {
  it.todo('loadProverArtifacts loads from ENCLAVE_PROVING_ARTIFACTS_PATH without network calls');
  it.todo('prove() returns exactly 128-byte compressed Groth16 output');
  it.todo('prove() returns proof with a, b, c fields via uncompressed decomposition');
  it.todo('[live] Node WASM prover generates valid proof for witness-1real-1null fixture');
});
```

**packages/agent/src/__tests__/witness.test.ts:**

```typescript
import { describe, it } from '@jest/globals';

describe('Witness construction — Model X (SDK-07)', () => {
  it.todo('two agents from the same org (same orgSpendingPrivKey) produce proofs with identical inPrivateKey fields');
  it.todo('buildWitnessInputs sets inPrivateKey[0] === inPrivateKey[1] === orgSpendingPrivKey for Model X');
  it.todo('buildWitnessInputs sets inAmount[0] = "0" for the null slot');
  it.todo('buildWitnessInputs sets inBlinding[0] from the null note blinding field');
  it.todo('buildWitnessInputs strips _pool08_evidence and inPublicKey before calling compute_witness');
});
```

**packages/agent/src/__tests__/fetch-interceptor.test.ts:**

```typescript
import { describe, it } from '@jest/globals';

describe('agent.fetch() intercept (SDK-01)', () => {
  it.todo('returns response directly when status is not 402');
  it.todo('completes 402 -> parsePaymentRequirements -> selectNote -> prove -> POST /settle -> retry cycle');
  it.todo('attaches Authorization: Bearer <authKey> to /settle POST');
  it.todo('retries original request with X-PAYMENT header containing txHash from /settle');
  it.todo('throws EnclavePaymentError({ reason: "no_funds" }) when no note covers maxAmountRequired');
  it.todo('throws EnclavePaymentError({ reason: "facilitator_rejected" }) when /settle returns non-200');
  it.todo('throws EnclavePaymentError({ reason: "retry_402" }) when retry still returns 402');
  it.todo('fixture mode: skips prover and uses pre-generated proof when ENCLAVE_FIXTURE_PATH is set and URL matches');
  it.todo('fixture mode: falls back to live proving on fixture cache miss (logs WARN)');
});
```
  </action>
  <verify>
    <automated>cd /Users/francoperez/repos/stellar-projects/stellar-enclave && npx jest packages/agent/src/__tests__/ --no-coverage --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - packages/agent/src/types.ts contains `export class EnclavePaymentError`
    - packages/agent/src/types.ts contains `export interface EnclaveNote`
    - packages/agent/src/types.ts contains `export interface AgentBundle`
    - packages/agent/src/types.ts contains `export interface ExtData`
    - packages/agent/src/index.ts does NOT contain `PHASE_0_STUB`
    - packages/agent/src/index.ts contains `export { EnclavePaymentError }`
    - packages/agent/src/__tests__/logger.test.ts exists and contains `describe('Logger redaction`
    - packages/agent/src/__tests__/prover.test.ts exists and contains `describe('Prover wrapper`
    - packages/agent/src/__tests__/witness.test.ts exists and contains `describe('Witness construction`
    - packages/agent/src/__tests__/fetch-interceptor.test.ts exists and contains `describe('agent.fetch()`
    - Jest command exits 0 (all tests pass/skip — no failures from stubs)
  </acceptance_criteria>
  <done>All module files exist with correct types; 4 test files with todo stubs all pass; Jest exits 0</done>
</task>

</tasks>

<verification>
After both tasks:
1. `cd /Users/francoperez/repos/stellar-projects/stellar-enclave && npx jest packages/agent/src/__tests__/ --no-coverage` exits 0
2. `grep "\.enclave\.json" /Users/francoperez/repos/stellar-projects/stellar-enclave/.gitignore` returns a hit
3. `grep "notes\.json" /Users/francoperez/repos/stellar-projects/stellar-enclave/.gitignore` returns a hit
4. All 7 source files exist under packages/agent/src/
</verification>

<success_criteria>
- Jest infrastructure is installed and runnable in packages/agent/
- All 4 test stub files exist with meaningful describe/it.todo blocks covering all 7 SDK requirements
- All source module files exist as typed stubs (no untyped `any` exports, correct return types)
- EnclaveNote, AgentBundle, ExtData, EnclavePaymentError, FixtureIndex types are exported from index.ts
- .gitignore blocks *.enclave.json and *-notes.json
- `npx jest packages/agent/src/__tests__/ --no-coverage` exits 0 with all tests shown as todo/pending
</success_criteria>

<output>
After completion, create `.planning/phases/03-agent-sdk-enclave-agent/03-01-SUMMARY.md`
</output>
